import 'package:flutter/material.dart';

class Generale extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('This generale Page', style: TextStyle(fontSize: 24)),
      ),
    );
  }
}