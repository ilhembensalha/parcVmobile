import 'package:flutter/material.dart';

class TransitPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('This is the Transit Page', style: TextStyle(fontSize: 24)),
      ),
    );
  }
}